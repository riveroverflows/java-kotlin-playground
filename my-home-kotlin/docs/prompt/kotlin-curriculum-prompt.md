# Kotlin 학습 커리큘럼 구성 프롬프트

## 배경 정보

### 학습자 프로필
- **Kotlin 경험**: 완전 처음 (문법을 전혀 모름)
- **Java 경험**: 콘솔 게임 프로젝트 구현 경험 있음
- **학습 동기**: 다음주 Kotlin+Spring 교육 시작 예정, 따라갈 수 있는 기본기 필요
- **학습 기간**: 1-2주 (빠른 페이스)

### 학습 목표
1. **기본 문법 습득**: Kotlin 기본 문법을 Java와 비교하며 학습
2. **Null Safety**: 처음부터 Kotlin의 null 안전성 시스템 습득
3. **Java→Kotlin 변환 능력**: 기존 Java 코드를 Kotlin으로 마이그레이션하는 능력
4. **Spring 교육 대비**: Kotlin+Spring 교육을 따라갈 수 있는 수준

### 학습 방식
- **Java 패키지 전체를 Kotlin으로 점진적 변환**
- **최종 목표**: Kotlin 실행 결과 = Java 실행 결과
- Kotlin idiom을 적용한 개선 (단순 변환이 아님)

---

## 기존 Java 프로젝트 분석

### 프로젝트 구조
```
com.rofs.myhome/
├── main/          # 게임 진입점 및 메인 루프 (3개)
├── character/     # 캐릭터 계층 (5개)
├── area/          # 게임 영역 (7개)
├── item/          # 아이템 시스템 (7개)
├── inventory/     # 인벤토리 관리 (2개)
├── quest/         # 퀘스트/업적 시스템 (8개)
├── harvestgame/   # 미니게임 (4개)
├── timer/         # 비동기 타이머 (2개)
├── soundplayer/   # 오디오 시스템 (2개)
└── etc/           # 유틸리티 (3개)
```

### 전체 파일 목록 (43개)

#### etc/ (3개)
- `MyHomeConstants.java` - 게임 상수 정의
- `MyHomeUtils.java` - 유틸리티 메서드
- `ProgressBar.java` - 진행률 표시

#### item/ (7개)
- `ItemType.java` - 아이템 타입 enum
- `Item.java` - 아이템 추상 클래스
- `GrowthItem.java` - 성장 아이템 (Item 상속)
- `StoreItem.java` - 상점 아이템 (Item 상속)
- `CraftItem.java` - 제작 아이템 (Item 상속)
- `Potion.java` - 포션 (Item 상속)
- `ItemStorage.java` - 아이템 저장소

#### inventory/ (2개)
- `ItemEntry.java` - 인벤토리 엔트리
- `Inventory.java` - 인벤토리 관리

#### character/ (5개)
- `Character.java` - 캐릭터 추상 클래스
- `NPC.java` - NPC (Character 상속)
- `Player.java` - 플레이어 (Character 상속)
- `Merchant.java` - 상인 NPC (NPC 상속)
- `LevelUpThread.java` - 레벨업 스레드

#### quest/ (8개)
- `TitleInfo.java` - 칭호 정보 데이터
- `Title.java` - 칭호 클래스
- `TitleStorage.java` - 칭호 저장소
- `QuestInfo.java` - 퀘스트 정보 데이터
- `Quest.java` - 퀘스트 클래스
- `QuestStorage.java` - 퀘스트 저장소
- `QuestThread.java` - 퀘스트 체크 스레드
- `AchieveTitleThread.java` - 칭호 획득 스레드

#### harvestgame/ (4개)
- `MiniGamePiece.java` - 미니게임 피스 추상 클래스
- `Bear.java` - 곰 (MiniGamePiece 상속)
- `Fish.java` - 물고기 (MiniGamePiece 상속)
- `BearCatchesFishGame.java` - 물고기 잡기 게임

#### area/ (7개)
- `AreaType.java` - 영역 타입 enum
- `Area.java` - 영역 추상 클래스
- `Farm.java` - 농장 (Area 상속)
- `AnimalFarm.java` - 목장 (Area 상속)
- `Forest.java` - 숲 (Area 상속)
- `CraftShop.java` - 제작소 (Area 상속)
- `Arcade.java` - 오락실 (Area 상속)

#### timer/ (2개)
- `CultivateTimer.java` - 재배 타이머
- `StoreTimer.java` - 상점 타이머

#### soundplayer/ (2개)
- `SoundPlayerUsingClip.java` - 오디오 재생 클래스
- `SoundPlayerClipTest.java` - 오디오 테스트

#### main/ (3개)
- `Main.java` - 진입점
- `MyHome.java` - 메인 게임 클래스
- `MyHomeTester.java` - 테스터

### Java 개념별 사용 현황
| Java 개념 | 사용 위치 | Kotlin 변환 포인트 |
|-----------|-----------|-------------------|
| 상속/추상클래스 | Character, Item, Area, MiniGamePiece | abstract class, open 키워드 |
| 컬렉션 | ArrayList, HashSet | listOf, mutableListOf, setOf |
| Generics | List<ItemEntry> | 동일하나 variance 차이 |
| Enum | ItemType, AreaType | enum class + when |
| 스레딩 | LevelUpThread, QuestThread, AchieveTitleThread | (Runnable 유지, 추후 Coroutine) |
| Static | MyHomeUtils, MyHomeConstants | object, companion object |
| 인터페이스 | Runnable, LineListener | 인터페이스 구현 문법 |

---

## 변환 전략

### 점진적 변환 (Java/Kotlin 공존)

```yaml
변환_방식: 점진적 변환

각_파일_변환_절차:
  1. Java 파일 분석 (의존성 확인)
  2. Kotlin 파일 생성 (src/main/kotlin/com/rofs/myhome/...)
  3. 컴파일 확인 (Java에서 Kotlin 호출 가능)
  4. 해당 Java 파일 삭제
  5. 전체 컴파일 확인

검증:
  - 매 파일 변환 후: ./gradlew compileKotlin compileJava
  - 매 단계 완료 후: ./gradlew run (게임 실행 테스트)
  - 최종: Java 실행 결과 = Kotlin 실행 결과
```

### 의존성 기반 복잡도 분류 (11단계)

```yaml
Phase_1_기반 (의존성 없음):
  - MyHomeConstants, ItemType, AreaType, TitleInfo
  - 난이도: 가장 쉬움 (다른 클래스에 의존하지 않음)

Phase_2_베이스클래스:
  - MyHomeUtils, Item, Character, MiniGamePiece
  - 난이도: 낮음 (Phase 1에만 의존)

Phase_3_인벤토리:
  - ItemEntry, Inventory
  - 난이도: 낮음 (Item에 의존)

Phase_4_아이템서브클래스:
  - GrowthItem, StoreItem, CraftItem, Potion
  - 난이도: 중간 (Item 상속)

Phase_5_캐릭터서브클래스:
  - NPC, Player, Merchant
  - 난이도: 중간 (Character 상속, Inventory 사용)

Phase_6_퀘스트:
  - QuestInfo, Quest, Title, TitleStorage, QuestStorage
  - 난이도: 중간 (컬렉션 활용)

Phase_7_게임메카닉:
  - ItemStorage, Bear, Fish, BearCatchesFishGame
  - CultivateTimer, StoreTimer, ProgressBar
  - 난이도: 중간 (게임 로직, 타이머)

Phase_8_영역:
  - Area, Farm, AnimalFarm, Forest, CraftShop, Arcade
  - 난이도: 높음 (복잡한 상호작용)

Phase_9_사운드:
  - SoundPlayerUsingClip, SoundPlayerClipTest
  - 난이도: 중간 (Java 라이브러리 연동)

Phase_10_스레딩:
  - LevelUpThread, QuestThread, AchieveTitleThread
  - 난이도: 중간 (Runnable 구현)

Phase_11_메인:
  - MyHome, MyHomeTester, Main
  - 난이도: 높음 (전체 통합)
```

---

## 커리큘럼 구성 요청

다음 조건에 맞는 **11단계 Kotlin 학습 커리큘럼**을 구성해줘:

### 필수 요구사항
1. **의존성 기반 순서**: 의존성 없는 파일부터 시작해서 의존하는 파일 순으로 진행
2. **Java 비교 학습**: 각 개념을 Java와 비교하며 설명
3. **Null Safety 통합**: 모든 단계에서 null 안전성을 함께 다룸
4. **실습 중심**: 이론 설명 후 바로 Java→Kotlin 변환 실습
5. **검증 포함**: 매 파일/단계마다 컴파일 및 실행 검증

### 코드 제외 규칙 (중요)
커리큘럼 문서에는 **코드를 포함하지 않음**:
- ❌ 예시 코드 블록
- ❌ Java/Kotlin 비교 코드
- ❌ 완성 코드 또는 정답 코드
- ❌ 코드 스니펫

**허용되는 경우** (최소한으로):
- ✅ 인라인 키워드 언급: `val`, `var`, `data class` 등
- ✅ 문법 형식 설명: "함수는 `fun 함수명(파라미터): 반환타입` 형식"
- ✅ 파일명/클래스명 참조: `Potion.java`, `ItemEntry`
- ✅ 명령어: `./gradlew compileKotlin`

**이유**:
- 코드는 멘토가 학습 진행 시 실시간으로 제공
- 커리큘럼은 "무엇을 배울지"만 정의하고, "어떻게"는 멘토 역할에서 담당

### 각 단계별 포함 내용
1. **학습 목표**: 이 단계에서 배울 핵심 개념
2. **핵심 개념**: 배울 Kotlin 개념 목록 (코드 없이 개념명과 간단한 설명)
3. **Java와 차이점**: 개념적 차이점 설명 (코드 없이 텍스트로)
4. **Null Safety 적용**: 해당 개념에서의 null 처리 방법 (개념 설명만)
5. **변환 실습 파일**: 해당 단계에서 변환할 Java 파일 지정
6. **체크포인트**: 학습 완료 확인을 위한 검증 과제

### 변환 실습 파일 순서 (11단계)

| 단계 | 파일 | 누적 | 핵심 학습 |
|------|------|------|----------|
| 1 | MyHomeConstants, ItemType, AreaType, TitleInfo | 4 | val/var, enum class, data class |
| 2 | MyHomeUtils, Item, Character, MiniGamePiece | 8 | object, abstract class, open |
| 3 | ItemEntry, Inventory | 10 | 다른 Kotlin 클래스 참조, nullable |
| 4 | GrowthItem, StoreItem, CraftItem, Potion | 14 | 상속, override, super |
| 5 | NPC, Player, Merchant | 17 | 복잡한 클래스 변환 |
| 6 | QuestInfo, Quest, Title, TitleStorage, QuestStorage | 22 | 컬렉션 (mutableListOf) |
| 7 | ItemStorage, Bear, Fish, BearCatchesFishGame, CultivateTimer, StoreTimer, ProgressBar | 29 | 2D 배열, 람다, when |
| 8 | Area, Farm, AnimalFarm, Forest, CraftShop, Arcade | 35 | 추상 클래스 상속 |
| 9 | SoundPlayerUsingClip, SoundPlayerClipTest | 37 | Java 라이브러리 연동 |
| 10 | LevelUpThread, QuestThread, AchieveTitleThread | 40 | Runnable 구현 |
| 11 | MyHome, MyHomeTester, Main | 43 | 전체 통합, 진입점 |

### 제외 사항 (기본기 이후 학습)
- Coroutines (스레딩 관련은 Runnable 유지)
- 고급 함수형 프로그래밍 (sequence, flow)
- Kotlin DSL
- 리플렉션

---

## 저장 위치

생성된 커리큘럼 문서는 다음 경로에 저장:
- **커리큘럼 문서**: `docs/curriculum/kotlin-learning-curriculum.md`

---

## 역할 연계

이 프롬프트로 생성된 커리큘럼은 다음 역할들과 연계하여 사용됨:

### 역할 흐름
```
[커리큘럼 생성] → [멘토 역할] → [코드리뷰어 역할]
```

1. **커리큘럼 생성** (이 프롬프트)
   - 학습 계획 및 단계별 목표 정의
   - 체크박스로 진행상황 추적 가능

2. **멘토 역할** (별도 프롬프트)
   - 커리큘럼 문서 기반으로 학습 진행
   - "학습 진행 상태" 섹션 업데이트
   - 학습자 질문 응답 및 개념 설명

3. **코드리뷰어 역할** (별도 프롬프트)
   - 변환된 Kotlin 코드 리뷰
   - Kotlin idiom 적용 여부 피드백
   - 개선점 제안

---

## 출력 형식

```markdown
# Kotlin 학습 커리큘럼

## 단계 1: [제목]
- [ ] 학습 시작
- [ ] 이론 학습 완료
- [ ] 실습 완료
- [ ] 체크포인트 통과

### 학습 목표
- 목표 1
- 목표 2

### 핵심 개념
- `개념명`: 간단한 설명 (코드 없이)
- `개념명`: 간단한 설명

### Java와 차이점
- 차이점 1 (텍스트 설명)
- 차이점 2 (텍스트 설명)

### Null Safety
- null 안전성 관련 개념 설명 (코드 없이)

### 변환 실습
- 변환할 파일: `파일명.java`
- 적용할 Kotlin 개념: 개념1, 개념2, ...

### 검증
- 컴파일: `./gradlew compileKotlin compileJava`
- 실행: `./gradlew run`

### 체크포인트
- [ ] 과제 1
- [ ] 과제 2

## 단계 2: [제목]
...

---

## 학습 진행 상태 (Claude Code 멘토 업데이트 영역)

> ⚠️ 이 섹션은 Claude Code 멘토가 학습 진행 중 업데이트합니다.
> 학습자는 이 섹션을 통해 진행 상황을 확인할 수 있습니다.

### 현재 상태
- **진행 단계**: 단계 X
- **변환 완료 파일**: X/43
- **마지막 학습일**: YYYY-MM-DD
- **다음 학습 예정**: [내용]

### 세션 로그
| 날짜 | 시간 | 다룬 내용 | 완료 여부 |
|------|------|-----------|-----------|
| YYYY-MM-DD | HH:MM | 내용 | ✅/🔄 |

### 학습 노트
> 멘토가 기록하는 학습 중 발견한 인사이트, 주의사항, 학습자 특성 등

- (학습 진행 시 기록)

### Q&A 기록
> 학습 중 나온 질문과 답변 기록

**Q**: 학습자 질문
**A**: 멘토 답변

---
```

---

## 추가 컨텍스트

### Spring 교육 대비 필수 개념
- data class (DTO, Entity)
- nullable types (요청/응답 처리)
- companion object (Factory 패턴)
- extension function (유틸리티)
- 람다와 고차함수 (함수형 엔드포인트)

### 검증 방법

#### 매 파일 변환 후
```bash
./gradlew compileKotlin compileJava
```

#### 매 단계 완료 후
```bash
./gradlew run  # 게임 실행 테스트
```

#### 최종 검증
1. Java 버전 실행 → 특정 시나리오 수행 → 결과 기록
2. Kotlin 버전 실행 → 동일 시나리오 수행 → 결과 비교
3. 동일 동작 확인

### 학습 리소스 (참고용)
- Kotlin 공식 문서: https://kotlinlang.org/docs
- Java to Kotlin 가이드: https://kotlinlang.org/docs/java-to-kotlin-idioms-strings.html
